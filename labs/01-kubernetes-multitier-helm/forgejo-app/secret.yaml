apiVersion: v1
kind: Secret
metadata:
  name: forgejo-secret
# NOTE: For local lab use only. Must match the MARIADB_PASSWORD in
# forgejo-db/secret.yaml. Generate with: echo -n "your-password" | base64
data:
  FORGEJO__database__PASSWD: "<base64-encoded>"
